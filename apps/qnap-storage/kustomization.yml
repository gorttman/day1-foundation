apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # qnap-books + qnap-calibre-web-live disabled 2026-09-03 with calibre-web -
  # both carried a stuck deletionTimestamp from the 2026-08-30 churn.
  # reclaimPolicy is Retain, so the QNAP data is untouched.
  # - qnap-books-pv.yml
  - qnap-vault-pv.yml
  - qnap-immich-pv.yml
  - qnap-paperless-pv.yml
  # qnap-pihole-live disabled 2026-09-03: pihole moved to the local-path
  # pihole-data-local PVC, the live PV was deleted, and git was the only
  # thing still declaring it.
  # - qnap-pihole-live-pv.yml
  # - qnap-calibre-web-live-pv.yml
