# No resources declared here on purpose - confirmed via the inventory
# pass (README.md's "Inventory" section) that both legacy portforward
# and routing (static routes) are genuinely empty (0 entries each) on
# this console. Re-confirmed during this stage (HISTORY.md #11) - still
# empty. Nothing to import, nothing to write.
#
# If port forwards or static routes are ever actually configured on
# this console, this file is where unifi_port_forward/
# unifi_static_route resources belong.
